{
  "name": "video-crop-tool",
  "version": "7.0.0",
  "description": "Studio-grade video crop and QC tool",
  "author": "Your Name",
  "main": "main.js",

  "scripts": {
    "start": "electron .",
    "build": "electron-builder"
  },

  "build": {
    "appId": "com.videocrop.tool",
    "productName": "Video Crop Tool",

    "directories": {
      "output": "dist"
    },

    "files": [
      "main.js",
      "preload.js",
      "backend/**/*",
      "public/**/*",
      "src/**/*",
      "data/**/*",
      "package.json"
    ],

    "extraResources": [
      {
        "from": "resources/mpv",
        "to": "mpv"
      }
    ],

    "publish": null,

    "win": {
      "target": "nsis"
    },

    "nsis": {
      "oneClick": true,
      "perMachine": false
    },

    "npmRebuild": false
  },

  "dependencies": {
    "fluent-ffmpeg": "^2.1.2",
    "ffmpeg-static": "^5.2.0"
  },

  "devDependencies": {
    "electron": "^28.3.3",
    "electron-builder": "^24.13.3"
  }
}
